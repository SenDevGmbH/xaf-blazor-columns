name: Publish to NuGet

on:
  workflow_dispatch:
  push:
    branches: [ "master", "main" ]

jobs:
  build-and-publish:
    uses: SenDevGmbH/github-workflows/.github/workflows/publish.yml@main
    with:
      solution-path: ./src/SenDev.Xaf.Blazor.Columns.slnx
      package-name-pattern: SenDev.Xaf.Blazor.Columns
    secrets: inherit
